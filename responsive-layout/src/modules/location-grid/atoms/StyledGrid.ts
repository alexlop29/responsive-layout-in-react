import Grid from "@mui/material/Unstable_Grid2";
import styled from "@emotion/styled";

/*
NOTE: (alopez) Gap on the right of the Grid.
*/
const StyledGrid = styled(Grid)`
  background-color: #cc0000;
`;

export { StyledGrid };
